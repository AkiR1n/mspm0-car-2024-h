# 七路循迹主逻辑方案

## 目标

在当前底盘、编码器、速度环、七路循迹、IMU 已接入的基础上，先实现一版可靠的七路巡线主逻辑，再为后续无线区和 IMU 接管预留接口。

当前阶段不追求一次把所有赛题情况做满，优先实现：

- 普通黑线稳定巡线
- 丢线后可按方向回找
- 弯道时自动降速
- 与现有 `control_task` 和速度环兼容

## 当前系统现状

当前代码已经具备以下基础件：

- 七路循迹传感器可输出 `line_bits`、`line_detected`、`line_position`
- `sensor_task` 已负责周期采样并写入 `app_state`
- `control_task` 已能接收 `v_mps`、`w_radps` 并走到底盘速度闭环
- `main_task` 已存在，但目前只是安全占位
- `LineController` 已封装线路偏差 PID

因此，主逻辑不需要直接操作左右轮占空比，也不需要重写速度环。`main_task` 只需要根据七路循迹结果输出合适的：

- `stop`
- `enable_closed_loop`
- `v_mps`
- `w_radps`

## 可选方案

### 方案一：纯位置 PID

控制方法：

- 输入只使用 `line_position`
- 输出 `w = line_pid(position)`
- 前进速度 `v` 固定

优点：

- 实现最快
- 第一版最容易直接跑起来

缺点：

- 丢线恢复能力弱
- 急弯、十字、全黑等特殊位型不鲁棒
- 不适合直接作为最终比赛方案

### 方案二：位型状态机 + 位置 PID

控制方法：

- `line_bits` 用于判断当前线路状态
- `line_position` 用于连续误差细调
- 正常状态下 `w = line_pid(position)`
- 特殊位型切换为专用策略

典型状态：

- `TRACK`
- `LOST_LEFT`
- `LOST_RIGHT`
- `CROSS`
- `ALL_BLACK`
- `SEARCH`

优点：

- 兼顾连续控制与离散判断
- 能显著增强丢线、急弯、特殊路段处理能力
- 适合作为当前主方案

缺点：

- 比纯 PID 多一层状态机设计

### 方案三：位型状态机 + 位置 PID + IMU 接管

控制方法：

- 普通黑线区：七路巡线
- 无线区或断线区：切换到 IMU 角度保持
- 重回有线区后：重新捕线并恢复巡线

优点：

- 最贴近最终赛题目标

缺点：

- 前提是普通七路巡线已经足够稳定
- 如果过早接入 IMU，会把基础问题复杂化

## 推荐方案

当前推荐采用方案二：

**位型状态机 + 位置 PID + 速度调度**

原因：

- 现有架构已经支持 `main_task -> v,w -> control_task -> 速度环`
- 七路信息已经够用，不需要再绕到底层轮速接口
- 该方案实现成本适中，但鲁棒性明显高于纯 PID
- 后续接入 IMU 时，可以自然扩展为方案三

## 控制接口建议

主任务只输出底盘级命令，不直接操作左右轮占空比。

建议保持如下链路：

`main_task -> app_state command(v,w) -> control_task -> Chassis_SetTwist() -> 速度环`

这样做的好处：

- 保持架构清晰
- 巡线逻辑和执行层解耦
- 后续接入 IMU、转角控制、无线区控制时都更容易扩展

## 第一版落地建议

### 1. 主循环周期

建议把 `main_task` 周期改为：

- 优先 `10 ms`
- 最多不超过 `20 ms`

`50 ms` 对巡线来说偏慢，弯道修正容易滞后。

### 2. 第一版状态

第一版不需要把所有复杂情况一次做满，建议先实现：

- `TRACK`
- `LOST_LEFT`
- `LOST_RIGHT`

### 3. 正常巡线逻辑

当检测到线时：

- `state = TRACK`
- `last_seen_error = line_position`
- `w = line_pid(line_position)`
- `v = speed_schedule(abs(line_position), line_bits)`

说明：

- `line_position` 目前范围大致为 `-30 ~ 30`
- 线在左侧时 `line_position < 0`
- 此时 `LineController_Update()` 输出应驱动车体向左纠偏

### 4. 丢线回找逻辑

当 `line_detected == 0` 时：

- 如果最近一次误差 `last_seen_error < 0`，说明线从左边丢失，进入 `LOST_LEFT`
- 如果最近一次误差 `last_seen_error > 0`，说明线从右边丢失，进入 `LOST_RIGHT`

回找阶段建议：

- 前进速度 `v` 降低到很小
- 给固定搜索角速度 `w`
- 一旦重新检测到线，立即回到 `TRACK`

### 5. 速度调度

不建议固定前进速度。

建议根据误差大小或位型自动降速：

- 误差小、位型居中：高速
- 误差中等、位型偏移：中速
- 边缘位型、急弯、回找：低速

可以从最简单的规则开始：

- `abs(position) <= 5`：高速
- `5 < abs(position) <= 15`：中速
- `abs(position) > 15`：低速

后续再根据实车效果细调。

## PID 建议

第一版线路控制 PID 建议：

- 先只用 `kp`
- `ki = 0`
- 必要时再加少量 `kd`

原因：

- 积分在巡线中很容易在丢线或急弯时积累错误状态
- 第一版先确认比例环方向、速度调度、丢线恢复逻辑

因此，第一阶段的调试顺序应为：

1. 确认 `line_position -> w` 方向正确
2. 用纯 `P` 让小车能贴线跑
3. 加速度调度
4. 必要时加少量 `D`
5. 最后再决定是否引入 `I`

## 暂不建议立即加入的内容

以下内容不建议在第一版同时实现：

- IMU 常态参与巡线
- 十字路口复杂决策
- 起终点判定
- 多段赛道任务编排
- 无线区完整策略

这些内容应建立在基础七路巡线已经稳定的前提上。

## 后续扩展方向

当普通巡线稳定后，可以继续扩展：

### 无线区接管

- 明确进入无线区时锁定 `yaw_ref`
- 进入 IMU 角度保持模式
- 在指定条件下重新捕线

### 特殊位型识别

- `0000000`：丢线
- `1111111`：全黑或特殊区域
- 中间连续命中：正常巡线
- 边缘命中：急弯或偏线

### 路口策略

- 十字口识别
- 左右分支判定
- 指定动作决策

## 当前推荐实施顺序

1. 把 `main_task` 周期改为 `10 ms`
2. 实现 `TRACK / LOST_LEFT / LOST_RIGHT`
3. 用 `line_position` 驱动 `w`
4. 加入基于误差的速度调度
5. 实车调通普通巡线
6. 再扩展 IMU 无线区逻辑

## 实施原则

当前阶段的核心原则是：

- 先把普通七路巡线跑稳定
- 再把 IMU 用在必须用的地方
- 主任务只做高层决策，不下沉到轮子控制细节

这能保证系统复杂度可控，也更符合当前代码架构。
