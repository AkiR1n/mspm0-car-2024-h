The `targetConfigs` folder contains target-configuration (`.ccxml`) files for
flashing and debugging with XDS110.

`MSPM0G3507.ccxml` is copied from the known-good `mspm0-modules-car-25`
project so `mspm0-car-2026` can be flashed directly without borrowing another
project's target configuration.
